{
  "name": "backend",
  "description": "Backend template for MERN stack project.",
  "scripts": {
    "test:watch": "jest -c jest.config.js --runInBand --watch --",
    "test": "jest -c jest.config.js --runInBand --",
    "coverage": "jest -c jest.config.js --runInBand --coverage",
    "lint": "eslint --config .eslintrc.js \"./src/**/*.js\" \"tests/**/*.js\"",
    "start": "node src/server.js",
    "dev": "nodemon ./src/server.js",
    "doc": "jsdoc -c jsdoc.config.json -r"
  },
  "repository": {
    "type": "git",
    "url": "git+httmls://github.com/busycaesar/MERN_Stack_Template/Backend.git"
  },
  "author": "Dev Jigishkumar Shah",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.2.0"
  },
  "devDependencies": {
    "better-docs": "^2.7.3",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jsdoc": "^4.0.2",
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4"
  }
}
